"use client";
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#aa0082",
    },
    secondary: {
      main: "#edffef",
    },
  },
});

export default theme;
